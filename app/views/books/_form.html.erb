<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :book_title %>
    <%= form.text_field :book_title, id: :book_book_title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :book_author %>
  </div>

  <div class="field">
    <%= form.label :genre %>
    <%= form.text_field :genre, id: :book_genre %>
  </div>

  <div class="field">
    <%= form.label :reading_level %>
    <%= form.number_field :reading_level, id: :book_reading_level %>
  </div>

  <div class="field">
    <%= form.label :number_of_pages %>
    <%= form.number_field :number_of_pages, id: :book_number_of_pages %>
  </div>

    <div class="field">
    <%= form.label :images %>
    <%= form.text_field :images, id: :images %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
